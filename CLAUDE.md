@AGENTS.md

# Standing Instructions

- At the end of every task, run `git status`, then commit and push all changes to GitHub — except `prd.md`, `architecture.md`, `rules.md`, `phases.md`, and `design.md`, which are never staged or committed by you per rules.md.
